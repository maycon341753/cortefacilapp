# Bloquear acesso direto a todos os arquivos da pasta database
Require all denied

# Proteger arquivos SQL
<Files "*.sql">
    Require all denied
</Files>

# Proteger arquivos de documentação
<Files "*.md">
    Require all denied
</Files>

# Bloquear listagem de diretório
Options -Indexes

# Proteger contra download direto
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^.*$ - [F,L]
</IfModule>